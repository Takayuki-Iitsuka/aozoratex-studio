\documentclass[lualatex, paper={90mm, 160mm}]{jlreq}
\usepackage{tikz}
\usepackage{lltjext}
\usepackage{xcolor}
\usetikzlibrary{calc}

% 濃いカラーパレットへの変更
\definecolor{BgGray}{HTML}{F2F2F2}      % 背景：据え置き
\definecolor{DeepSlate}{HTML}{2F4F4F}   % 曲線1：ダークスレートグレー（より濃く）
\definecolor{MidnightBlue}{HTML}{191970} % 曲線2：ミッドナイトブルー（より濃く）
\definecolor{Charcoal}{HTML}{36454F}    % 曲線3：チャコールグレー（シルバーから変更）

\begin{document}
\pagestyle{empty} 

\begin{tikzpicture}[remember picture, overlay]
  \fill[BgGray] (current page.south west) rectangle (current page.north east);

  % 不透明度を0.6から0.9に引き上げ、色をはっきりさせる
  \begin{scope}[shift={(current page.center)}, opacity=0.9]
    
    % 曲線1: line widthを0.8ptから1.0ptへ
    \draw[DeepSlate, line width=1.0pt, smooth, samples=200, domain=0:360] 
      plot ({35 * sin(3 * \x)}, {60 * sin(2 * \x + 90)});

    % 曲線2: line widthを0.6ptから0.8ptへ
    \draw[MidnightBlue, line width=0.8pt, smooth, samples=300, domain=0:360] 
      plot ({42 * sin(5 * \x)}, {70 * sin(4 * \x + 45)});

    % 曲線3: line widthを0.5ptから0.7ptへ
    \draw[Charcoal, line width=0.7pt, smooth, samples=300, domain=0:360] 
      plot ({30 * sin(3 * \x)}, {55 * sin(5 * \x)});
  \end{scope}

  % 枠線とテキストは既存のまま
  \draw[line width=1.2pt, color=black!60] ($(current page.south west) + (6mm, 6mm)$) rectangle ($(current page.north east) - (6mm, 6mm)$);
  \draw[line width=0.3pt, color=black!60] ($(current page.south west) + (7.5mm, 7.5mm)$) rectangle ($(current page.north east) - (7.5mm, 7.5mm)$);

  \node[anchor=north] at ($(current page.center) + (15mm, 50mm)$) {
    \begin{minipage}<t>{15em}
      \begin{center}
        {\fontsize{28pt}{40pt}\selectfont \textcolor{black!80}{走れメロス}\par}
      \end{center}
    \end{minipage}
  };

  \node[anchor=north] at ($(current page.center) + (-15mm, -10mm)$) {
    \begin{minipage}<t>{10em}
      \begin{center}
        {\fontsize{16pt}{24pt}\selectfont \textcolor{black!80}{太宰 治}\par}
      \end{center}
    \end{minipage}
  };
\end{tikzpicture}

\end{document}